# Generic.
backend = "glx";
vsync = "opengl";
unredir-if-possible = false;

# Shadows.
no-dock-shadow = true;
no-dnd-shadow = true;
clear-shadow = true;
shadow-opacity = 0;

# Windows.
fading = true;
fade-delta = 2;
