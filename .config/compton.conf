# Generic.
backend = "glx";
vsync = "opengl-mswc";
unredir-if-possible = false;
paint-on-overlay = true;

# Shadows.
no-dock-shadow = true;
no-dnd-shadow = true;
clear-shadow = true;
shadow-opacity = 0;

# Windows.
fading = true;
fade-delta = 2;
